# Add your own tasks in files placed in lib/tasks ending in .rake,
# for example lib/tasks/capistrano.rake, and they will automatically be available to Rake.

require './lib/wg-sneak-agent'

task :foo do
  puts "foo"
  5 / 0
end
require './lib/patch'
